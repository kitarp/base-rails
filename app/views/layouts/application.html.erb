<!DOCTYPE html>
<html>
  <head>
    <title>FinalProject</title>
    <%= csrf_meta_tags %>

    <!-- Expand the number of characters we can use in the document beyond basic ASCII 🎉 -->
    <meta charset="utf-8">

    <!-- Make it responsive to small screens -->
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>

  </head>

  <body>
      <nav>
      <ul>
        <li>
          <a href="/supervisor_sign_in">Sign In</a>
        </li>
        <li>
          <a href="/edit_supervisor_profile">Edit Profile</a>
        </li>
        <li>
          <a href="/supervisor_sign_out">Sign Out</a>
        </li>
        <li>
          <a href="/employees">All Employees</a>
        </li>
        <li>
          <a href="/insert_employee">Insert Employee</a>
        </li>
        <li>
          <%= session.fetch(:user_id)%>
        </li>
      </ul>
    </nav>
    <% if notice.present? %>
      <div style = "border: thin green solid; color: green">
        <%= notice%>
      </div>
    <% end %>  
    <% if alert.present? %>    
      <div style = "border: thin red solid; color: red">
        <%= alert%>
      </div>
    <% end %>  
    <%= yield %>
  </body>
</html>
